apiVersion: apps/v1
kind: Deployment
metadata: {name: ai-security-platform}
spec:
 replicas: 2
 selector: {matchLabels: {app: ai-security-platform}}
 template:
  metadata: {labels: {app: ai-security-platform}}
  spec:
   containers:
   - name: api
     image: ai-security-platform:latest
     ports: [{containerPort: 8000}]
     readinessProbe: {httpGet: {path: /health/ready, port: 8000}}
     livenessProbe: {httpGet: {path: /health/live, port: 8000}}
